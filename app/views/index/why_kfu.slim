#org
  .firstrow
    .centred
      .first_line.wow.fadeInDown
      .first_title.wow.fadeInDown
        p = raw(translate_field(@why_kfu_content, :name))
      .first_description.wow.fadeInDown
        p = raw(translate_field(@why_kfu_content, :content))
    .second_image
    .clear